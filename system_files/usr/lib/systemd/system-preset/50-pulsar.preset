# Only honoured by a fresh `bootc install`; a rebase onto this image never
# re-runs presets. The /etc wants symlink written at image build (see the
# Containerfile's finalize step) is what actually enables scx.service on a
# rebased system.
enable scx.service
disable NetworkManager-wait-online.service
